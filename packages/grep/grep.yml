sources:
  - name: grep
    subdir: sources
    url: 'https://mirror.ibcp.fr/pub/gnu/grep/grep-3.11.tar.xz'
    format: tar.xz
    extract_path: 'grep-3.11'
    version: '3.11'
    patch_path_strip: 1

packages:
  - name: grep
    architecture: x86_64
    from_source: grep
    tools_required:
      - host-gcc
    pkgs_required:
      - mlibc
    configure:
      - args:
          - '@THIS_SOURCE_DIR@/configure'
          - '--host=x86_64-petra'
          - '--prefix=/usr'
          - '--disable-nls'
          - '--disable-perl-regexp'
          - '--disable-rpath'
    build:
      - args: ['make', '-j@PARALLELISM@']
    install:
      - args: ['make', 'DESTDIR=@SYSROOT_DIR@', 'install']
      - args: |
          mkdir -p @SYSROOT_DIR@/bin
          ln -sf /usr/bin/grep @SYSROOT_DIR@/bin/grep 2>/dev/null || true
          ln -sf /usr/bin/egrep @SYSROOT_DIR@/bin/egrep 2>/dev/null || true
          ln -sf /usr/bin/fgrep @SYSROOT_DIR@/bin/fgrep 2>/dev/null || true
          echo "grep install completed"
